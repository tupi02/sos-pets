<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amandacruz.sospets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="                          SOSPET&apos;S"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        //Tela de abertura Android Studio //
        <activity android:name=".TelaUserAdmin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        //Toda Activity deve estar declarada no Manifest//
        <activity android:name=".TelaUsuario" />
        <activity android:name=".TelaAdmin" />
        <activity android:name=".TelaLocalizarAnimal" />
        <activity android:name=".TelaPrincipalAdmin" />
        <activity android:name=".TelaDenuncia"></activity>
    </application>

</manifest>